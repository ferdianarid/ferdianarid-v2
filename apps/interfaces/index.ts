export interface IStatistics {
    id: number
    count: number | string
    title: string
}

export interface IRecentProject {
    id: number
    fileImage: string
    title: string
    description: string
}